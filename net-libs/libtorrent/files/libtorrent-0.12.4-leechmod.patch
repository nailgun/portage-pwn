diff -ur libtorrent-0.12.4/configure libtorrentleech-0.12.4/configure
--- libtorrent-0.12.4/configure	2008-11-19 12:03:33.000000000 +0300
+++ libtorrentleech-0.12.4/configure	2009-04-02 18:09:30.000000000 +0400
@@ -2092,12 +2092,12 @@
 
 
 cat >>confdefs.h <<\_ACEOF
-#define PEER_NAME "-lt0C40-"
+#define PEER_NAME "-UT1770-"
 _ACEOF
 
 
 cat >>confdefs.h <<\_ACEOF
-#define PEER_VERSION "lt\x0C\x40"
+#define PEER_VERSION "UT\x17\x70"
 _ACEOF
 
 
diff -ur libtorrent-0.12.4/configure.ac libtorrentleech-0.12.4/configure.ac
--- libtorrent-0.12.4/configure.ac	2008-11-19 12:03:17.000000000 +0300
+++ libtorrentleech-0.12.4/configure.ac	2009-04-02 18:10:17.000000000 +0400
@@ -1,8 +1,8 @@
 AC_INIT(libtorrent, 0.12.4, jaris@ifi.uio.no)
 
 dnl Find a better way to do this
-AC_DEFINE(PEER_NAME, "-lt0C40-", Identifier that is part of the default peer id)
-AC_DEFINE(PEER_VERSION, "lt\x0C\x40", 4 byte client and version identifier for DHT)
+AC_DEFINE(PEER_NAME, "-UT1770-", Identifier that is part of the default peer id)
+AC_DEFINE(PEER_VERSION, "UT\x17\x70", 4 byte client and version identifier for DHT)
 
 LIBTORRENT_CURRENT=11
 LIBTORRENT_REVISION=4
diff -ur libtorrent-0.12.4/src/tracker/tracker_http.cc libtorrentleech-0.12.4/src/tracker/tracker_http.cc
--- libtorrent-0.12.4/src/tracker/tracker_http.cc	2008-05-07 16:19:13.000000000 +0400
+++ libtorrentleech-0.12.4/src/tracker/tracker_http.cc	2009-04-02 18:30:02.000000000 +0400
@@ -127,9 +127,9 @@
   if (manager->connection_manager()->listen_port())
     s << "&port=" << manager->connection_manager()->listen_port();
 
-  s << "&uploaded=" << info->uploaded_adjusted()
-    << "&downloaded=" << info->completed_adjusted()
-    << "&left=" << info->slot_left()();
+  s << "&uploaded=0"
+    << "&downloaded=0"
+    << "&left=" << info->slot_left()() + info->slot_completed()();
 
   switch(state) {
   case DownloadInfo::STARTED:
@@ -139,7 +139,7 @@
     s << "&event=stopped";
     break;
   case DownloadInfo::COMPLETED:
-    s << "&event=completed";
+    s << "&event=stopped";
     break;
   default:
     break;
diff -ur libtorrent-0.12.4/src/tracker/tracker_udp.cc libtorrentleech-0.12.4/src/tracker/tracker_udp.cc
--- libtorrent-0.12.4/src/tracker/tracker_udp.cc	2008-05-07 16:19:13.000000000 +0400
+++ libtorrentleech-0.12.4/src/tracker/tracker_udp.cc	2009-04-02 18:26:34.000000000 +0400
@@ -263,10 +263,10 @@
   m_writeBuffer->write_range(info->hash().begin(), info->hash().end());
   m_writeBuffer->write_range(info->local_id().begin(), info->local_id().end());
 
-  m_writeBuffer->write_64(info->completed_adjusted());
-  m_writeBuffer->write_64(info->slot_left()());
-  m_writeBuffer->write_64(info->uploaded_adjusted());
-  m_writeBuffer->write_32(m_sendState);
+  m_writeBuffer->write_64(0);
+  m_writeBuffer->write_64(info->slot_left()() + info->slot_completed()());
+  m_writeBuffer->write_64(0);
+  m_writeBuffer->write_32(m_sendState == DownloadInfo::COMPLETED ? DownloadInfo::STOPPED : m_sendState);
 
   const rak::socket_address* localAddress = rak::socket_address::cast_from(manager->connection_manager()->local_address());
 
